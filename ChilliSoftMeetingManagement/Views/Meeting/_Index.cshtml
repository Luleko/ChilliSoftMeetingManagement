@model IEnumerable<ChilliSoftMeetingManagement.Repository.EF.Meeting>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MeetingDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MeetingName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MeetingType.MeetingTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MeetingDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MeetingName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MeetingType.MeetingTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MeetingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MeetingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MeetingId })
        </td>
    </tr>
}

</table>
